# AdSense (never put secrets that must stay private here — pub/slot IDs are public)
VITE_ADSENSE_CLIENT=ca-pub-3733845304652661
VITE_ADSENSE_SLOT_LEFT=
VITE_ADSENSE_SLOT_RIGHT=
VITE_ADSENSE_SLOT_BOTTOM=

# false = labeled placeholders only (safe for local / pre-approval)
# Site ownership verification only needs the script in index.html — not this flag.
VITE_ADS_ENABLED=false

# true keeps Google test creatives (data-adtest=on). Set false only after approval + go-live.
VITE_ADS_TEST=true
